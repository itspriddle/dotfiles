#!/bin/sh
# Usage: addr [<if>]
# Show IPv4 address for interface <if> or all interfaces with
# no <if> given.
iface=$1
ifconfig ${iface:=-a} |
grep 'inet '          |
grep broadcast        |
awk '{ print $2 }'
