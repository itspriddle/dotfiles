#!/bin/sh
if [ $# -eq 0 ]; then
  echo "Usage: $0 [DATABASE]"
  echo
  exit 42
fi

DATABASE="$1"
mysql -e "CREATE DATABASE $DATABASE"
mysql -e "GRANT ALL PRIVILEGES ON $DATABASE.* TO root@localhost IDENTIFIED BY ''";
echo "Created database $DATABASE"

