# Ignore these dirs
--ignore-dir=build
--ignore-dir=coverage
--ignore-dir=_site
--ignore-dir=dump

# Colors
--color-match=bold red
--color-filename=bold yellow

# Add "uninteresting" filetypes
--type-set=plaintext=.md,.mdown,.markdown,.mkdn,.textile,.rst,.txt,.scss,.css,.coffee
--type-add=ruby=.gemspec,Gemfile,Rakefile

# only search with case sensitivity if there is mixed case
--smart-case

# Ignore these file types
--no-sql
