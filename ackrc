# Ignore these dirs
--ignore-dir=build
--ignore-dir=coverage
--ignore-dir=_site
--ignore-dir=dump

# Colors
--color-match=bold red
--color-filename=bold yellow

# Add "uninteresting" filetypes
--type-set=plaintext=.md,.mdown,.markdown,.mkdn,.textile,.rst,.txt
--type-set=plaintext=.scss,.css,.coffee

# only search with case sensitivity if there is mixed case
--smart-case
