#!/usr/bin/env bash

target="${1:-daily}"

case "$target" in
  hourly|daily|monthly|yearly)
    ;;
  *)
    echo "Usage: ${0##*/} hourly|daily|monthly|yearly" >&2
    exit 1
    ;;
esac

opnsense-api "vnstat/service/$target" | jq -r .response
